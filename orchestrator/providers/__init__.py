"""Providers package — LLM SDK wrappers (one module per vendor).

Onda 2 #2: api_executor.py renamed to providers/anthropic.py because it is,
in fact, an Anthropic SDK wrapper, not a generic executor.

Modules:
    providers.anthropic   Anthropic Claude API wrapper (was api_executor.py)
"""
